server {
    listen 80;
    # Catch-all: the public hostname is handled by your reverse proxy / TLS layer
    # (Synology, Caddy, Traefik, …). Set a specific server_name if you serve
    # nginx directly on a domain.
    server_name _;

    client_max_body_size 500M;

    # Backend API + HTML pages
    location / {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support (future)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Serve uploaded files directly (faster than going through Python)
    location /data/uploads/ {
        alias /data/uploads/;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
